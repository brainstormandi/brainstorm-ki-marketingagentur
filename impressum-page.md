# Task: Create Impressum Page

## Description
Create a new page for the Impressum (Imprint) and link it in the Footer. The page should follow the existing design system but be minimalist in layout.

## Goals
- Create `app/impressum/page.tsx` with the provided legal content.
- Ensure the design matches the rest of the site (Tailwind CSS, fonts).
- Update `app/components/Footer.tsx` to link to `/impressum`.
- Verify the layout is minimalist (Header, Content, Footer).

## Steps
1. [x] Create `app/impressum/page.tsx`.
2. [x] Create `app/datenschutz/page.tsx`.
3. [x] Update `app/components/Footer.tsx`.
4. [x] Verify the links and page layouts.

## Technical Details
- URL: `/impressum`
- Tech Stack: Next.js (App Router), Tailwind CSS
- Design: Premium minimalist, consistent with the main site.
